<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace=".dao.TStudentDao">

    <resultMap type=".entity.TStudent" id="TStudentMap">
        <result property="studentId" column="student_id" jdbcType="INTEGER"/>
        <result property="clazzId" column="clazz_id" jdbcType="INTEGER"/>
        <result property="studentName" column="student_name" jdbcType="VARCHAR"/>
        <result property="hometown" column="hometown" jdbcType="VARCHAR"/>
        <result property="birthday" column="birthday" jdbcType="OTHER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="TStudentMap">
        select
          student_id, clazz_id, student_name, hometown, birthday
        from springtest.t_student
        where student_id = #{studentId}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="TStudentMap">
        select
          student_id, clazz_id, student_name, hometown, birthday
        from springtest.t_student
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="TStudentMap">
        select
          student_id, clazz_id, student_name, hometown, birthday
        from springtest.t_student
        <where>
            <if test="studentId != null">
                and student_id = #{studentId}
            </if>
            <if test="clazzId != null">
                and clazz_id = #{clazzId}
            </if>
            <if test="studentName != null and studentName != ''">
                and student_name = #{studentName}
            </if>
            <if test="hometown != null and hometown != ''">
                and hometown = #{hometown}
            </if>
            <if test="birthday != null">
                and birthday = #{birthday}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="studentId" useGeneratedKeys="true">
        insert into springtest.t_student(clazz_id, student_name, hometown, birthday)
        values (#{clazzId}, #{studentName}, #{hometown}, #{birthday})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update springtest.t_student
        <set>
            <if test="clazzId != null">
                clazz_id = #{clazzId},
            </if>
            <if test="studentName != null and studentName != ''">
                student_name = #{studentName},
            </if>
            <if test="hometown != null and hometown != ''">
                hometown = #{hometown},
            </if>
            <if test="birthday != null">
                birthday = #{birthday},
            </if>
        </set>
        where student_id = #{studentId}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from springtest.t_student where student_id = #{studentId}
    </delete>

</mapper>